apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: group-5-paybox
  name: paybox-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  volumeName: paybox-postgres-pv
  storageClassName: paybox-local-storage
